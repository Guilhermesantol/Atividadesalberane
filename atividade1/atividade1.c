#include <stdio.h>

int main(){
	int a,b,c, soma = 0;
	a = 10;
	b = 5;
	c = 3;
	soma = soma + a;
	soma = soma + b;
	soma = soma + c;
	printf("o valor de a eh: %d\no de b eh: %d\no de c eh: %d\na soma de todos eles eh: %d", a, b, c, soma);
}