#include <stdio.h>
#include <string.h>

int main(){
	int tamanhodapalavra(){
	char palavra[] = "Hello World";
	size_t tamanho = strlen(palavra);
	printf("O tamanho da palavra é: %zu", tamanho);
	return 0;
	}

}